{{- if .Values.redis.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nimbusware-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nimbusware-redis
  template:
    metadata:
      labels:
        app: nimbusware-redis
    spec:
      containers:
        - name: redis
          image: {{ .Values.redis.image }}
          ports:
            - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: nimbusware-redis
spec:
  selector:
    app: nimbusware-redis
  ports:
    - port: 6379
      targetPort: 6379
{{- end }}
