# Acknowledgments

## Hardware fit (Odysseus)

Model fit tiers, governor caps, and ranked-model UX in `nimbusware_hw` adapt patterns from the Odysseus **hwfit** cookbook. We ship an offline `configs/hardware/model_catalog.json` and routing allowlist integration rather than a runtime HF download in CI.

## Other

See package READMEs and gitignored `nimbusware-orchestrator-local-plan.md` (repo root) for additional lineage notes.
