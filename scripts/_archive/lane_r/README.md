# Lane R mechanical refactor scripts (archived)

One-off scripts used during the Admin Console god-module splits (Lane R, 2025–2026). They are **not** part of day-to-day ops; kept for reference only. **Streamlit UI is retired** — scripts that import `streamlit` are historical and must not be run.

Active maintenance splits (Lane X) live in `scripts/split_*.py` at the repo root of `scripts/`.

Operational scripts remain in `scripts/` (e2e, install, postgres, replay, FAISS rebuild, etc.).
