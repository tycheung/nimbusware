# ADR 018: Continuous improvement council

## Status

Accepted (amended 2026-07 — expanded tracks).

## Decision

`build_repo_inventory` → weighted council vote → selected improvement track (`simplify`, `refactor_cohesion`, etc.).

## Amendment

Tracks now include `improve_coverage`, `discover_features`, `variant_experiment` (votes restored), plus `security_harden`, `performance_tune`, `architecture_revise`, `document_contracts`, `distill_artifacts`. RepoScope matrix updated (HARDEN excludes variant/architecture/perf). See [self-evolution.md](../product/self-evolution.md) and ADR 035.
