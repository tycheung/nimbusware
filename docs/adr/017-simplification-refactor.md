# ADR 017: Simplification refactor metrics

## Status

Accepted.

## Decision

`ComplexityIndex`, orphan/similarity/cohesion indexes feed maintenance backlog; `simplification.rubric` on SIMPLIFY track and `refactor.loc_accord` on refactor passes (`loc_accord_stage.py`); loc increases require council accord (see ADR 019).
