# Legacy theme reference for console_theme tests (web UI is default).
# White-label deferred — use [theme] keys below for local overrides only.
[theme]
base = "light"
primaryColor = "#1f77b4"
font = "sans serif"
