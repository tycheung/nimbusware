stack_id: fastapi_python
surface: api
display_name: Python FastAPI
workspace_subdir: backend
writer_role: backend_writer
allowed_globs:
  - "**/*.py"
  - "**/pyproject.toml"
  - "**/requirements.txt"
verify_commands:
  - ["python", "-m", "pytest", "-q"]
launch_framework_id: null
max_files: 5
max_loc: 200
agent_scaffold:
  system_prompt: "Implement Python FastAPI backend slices for the frozen stack manifest globs."
  description: "Auto-scaffolded from fastapi_python stack catalog"
