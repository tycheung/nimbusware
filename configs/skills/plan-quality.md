# Plan quality

- Each slice touches at most three paths under packages/.
- Acceptance criteria must be testable in one pytest invocation.
- Rationale must reference existing modules, not greenfield rewrites.
