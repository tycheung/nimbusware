# Optional profile: integrator gate on with ``project_tags`` that do not overlap the
# mapped bundle catalog tags (default map → auth-rbac-starter [auth, rbac]) → FAIL gate.
extends: default
version: 1
integrator_gate:
  enabled: true
  project_tags: [billing, stripe]
