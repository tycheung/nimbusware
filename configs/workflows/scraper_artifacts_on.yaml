# Optional profile: same policy shape as ``default`` but documents a positive on-disk
# scraper artifact cap when operators enable ``scraper_fetch``.
extends: default
version: 1
scraper_fetch:
  persist_artifacts_max_bytes_per_url: 65536
