# Agent evaluator with default persona + persona coverage critique for gap tests.
extends: default
version: 1
agent_evaluator:
  enabled: true
  persona_id: default
  llm_evaluation_enabled: true
  persona_coverage_critique:
    enabled: true
    stub: true
