# Optional profile: Integration Adapter Writer live metadata slice (§14 #13).
version: 1
integration_adapter_writer:
  enabled: true
  target_adapter_kind: api_bridge
  stub_only: false
integrator_gate:
  enabled: true
