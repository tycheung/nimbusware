# hermes_executor

Role-gated outbound HTTP and subprocess helpers for slice implement agents. Centralizes egress allowlists and timeout policy away from orchestrator mixins.

## Consumers

`hermes_orchestrator` micro-slice implement path; tests under `tests/unit/test_executor.py`.

Normative Hermes contract: gitignored `hermes-orchestrator-local-plan.md` at repo root.
