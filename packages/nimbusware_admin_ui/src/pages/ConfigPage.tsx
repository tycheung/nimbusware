export function ConfigPage() {
  return (
    <section>
      <h2>Configuration</h2>
      <p>Ollama, bundles, workflows, and system settings — use API-backed panels in upcoming builds.</p>
    </section>
  );
}
